root {
    # Override the default root matcher to render always mockup document nodetype in Mockup.active mode
    mockup {
        @position = 'end'
        condition = ${Configuration.setting('Networkteam.Neos.Mockup.active')}
        renderer = Networkteam.Neos.Mockup.Page
    }
}